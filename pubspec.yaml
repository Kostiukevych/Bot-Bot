name: binance_testnet_trader
description: "Futuristic HUD Trading Terminal for Binance Spot Testnet"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.2
  http: ^1.2.1
  web_socket_channel: ^2.4.5
  crypto: ^3.0.3
  fl_chart: ^0.68.0
  google_fonts: ^6.2.1
  intl: ^0.19.0
  sqflite: ^2.3.3+1
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
